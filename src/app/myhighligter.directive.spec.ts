import { MyhighligterDirective } from './myhighligter.directive';

describe('MyhighligterDirective', () => {
  it('should create an instance', () => {
    const directive = new MyhighligterDirective();
    expect(directive).toBeTruthy();
  });
});
